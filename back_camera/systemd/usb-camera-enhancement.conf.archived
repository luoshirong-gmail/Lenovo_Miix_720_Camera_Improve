# USB Camera Enhancement - v4l2loopback module options (归档)
# =====================================================================
# 归档演进 (2026-08-09):
#   方案 A 轮 (video_nr=99,16 + card_label 数组, 模块参数直接建 video16):
#     引号/转义空格解析问题 (card 截断 "OV5670") → 回滚
#   清理版 (当前部署): video_nr=99 无引号 — video16 由 ensure-device add
#     创建 (v4l2loopback.c AF 三控件无条件注册, 与 card_label 无关)。
# 部署位置: /etc/modprobe.d/usb-camera-enhancement.conf
options v4l2loopback exclusive_caps=0 max_width=3840 max_height=2160 max_buffers=16 card_label=EnhancedCamera video_nr=99
