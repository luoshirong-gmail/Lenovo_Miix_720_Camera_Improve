# OV5670 Virtual Camera - load loopback module at boot
# (module options live in /etc/modprobe.d/ov5670-virtual-camera.conf;
#  modules-load.d 只写模块名, options 行会被 systemd 忽略)
# v4l2loopback (2026-08-09: 改由 v4l2loopback-load.service 用 modprobe 加载, 参数完整解析)
